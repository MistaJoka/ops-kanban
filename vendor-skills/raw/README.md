# Raw external skills — audit-only archive

**Do not load in normal agent sessions.**

This directory holds verbatim copies of external SKILL.md and reference files. Sources are **populated** — see provenance in [`../SOURCE_NOTES.md`](../SOURCE_NOTES.md) and the file catalog in [`../INDEX.md`](../INDEX.md).

Daily work uses the distilled layer instead:

- [`docs/external-skills/distilled/`](../../docs/external-skills/distilled/)

Raw sources are excluded from default Cursor indexing via [`.cursorignore`](../../.cursorignore). Read raw files only when:

1. The user explicitly requests an audit or expansion review, or
2. You are reconciling distilled docs against original sources (compare against pinned SHAs in `SOURCE_NOTES.md`).

If raw content conflicts with [`AGENTS.md`](../../AGENTS.md) or canonical docs, canonical docs win.

**Archived upstream (curated subset):** gstack · claude-mem · anthropics/skills (frontend-design) · obra/superpowers · supplementary cursor/supabase refs.
